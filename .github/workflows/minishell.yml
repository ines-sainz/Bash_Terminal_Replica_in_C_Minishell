name: C/C++ CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Make
      run: make
    - name: Clean
      run: make clean
    - name: Make executable
      run: chmod +x minishell
    - name: List files in directory
      run: ls -l
    - name: Run de program
      run: bash -c "./minishell"
